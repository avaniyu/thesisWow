<UserControl x:Class="ClassLibrary.Views.View_TypingResults"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="controlTypingResults"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="txtTypingResults" Text="{Binding TypingResults}" FontSize="20"/>
        <Button x:Name="btnConfirm" Content="Confirmation" Command="{Binding ElementName=controlTypingResults, Path=CommandConfirm}" Grid.Row="1" Margin="10 0 10 0" FontSize="20"/>

    </Grid>
</UserControl>
